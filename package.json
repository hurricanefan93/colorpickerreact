{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain colorpickerreact.garretmorales.surge.sh",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "dependencies": {
    "browser-sync": "^2.18.8",
    "stylelint": "^7.10.1",
    "stylelint-config-standard": "^16.0.0",
    "surge": "^0.19.0"
  }
}
